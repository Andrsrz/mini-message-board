extends layout

block content
	h1= title
	p Note: This messages are not persistent.
	for message in messages
		h3(style="color: #5B5EA6") - #{message.user}
		h2 #{message.text}
		p #{message.added}

	form(method='POST' action='')
		button(type='submit' style='cursor: pointer') New Message
